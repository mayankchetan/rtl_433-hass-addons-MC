# 0.10.1

- MC fork: vendor rtl_433_mqtt_hass.py (merbanan/rtl_433 master @ 91b2ebdb)
  into this repo instead of fetching from a rtl_433 fork at build time.
- Re-applied local rain_start -> binary_sensor (moisture) mapping for the
  Fine Offset WS90 (not mapped upstream).
- Merged upstream pbkhrv/rtl_433-hass-addons through 0.10.0.

